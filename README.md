Calendar console application that stores different kinds of calendar items (meetings, tasks, appointments) 
and allows the user to query and print them using typed commands.
File export of calendar items into a tab-delimited .txt file.
File import of calendar items from a tab-delimited .txt file.
Uses custom exception class.
Parses tab-delimited data files.
